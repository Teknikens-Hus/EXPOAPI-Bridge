query GetBookingTypes{
  bookingTypes{
    totalNodeCount
    totalPageCount
    pageInfo{
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
    nodes{
      id
      name
      description
    }
  }
}